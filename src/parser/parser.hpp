#ifndef MGRAMMAR_PARSER_HPP
#define MGRAMMAR_PARSER_HPP

#include <string>

namespace grammar {
    namespace parser {
    
    bool parse(std::string src);
        
    } // namespace parser 

} // namespace grammar 


#endif